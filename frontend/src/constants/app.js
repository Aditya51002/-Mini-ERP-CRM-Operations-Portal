export const STORAGE_KEYS = Object.freeze({
  TOKEN: "mini_erp_token",
  USER: "mini_erp_user"
});

export const API_PATHS = Object.freeze({
  AUTH_LOGIN: "/auth/login",
  CUSTOMERS: "/customers",
  PRODUCTS: "/products",
  CHALLANS: "/challans",
  SUPPLIERS: "/suppliers",
  PURCHASE_ORDERS: "/purchase-orders"
});

export const PAGE_SIZE = Object.freeze({ DEFAULT: 20, COMPACT: 10, LOOKUP: 100 });
